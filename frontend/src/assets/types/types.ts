export type ahrefResponseObject = {
  text: string;
  url: string;
};

export type ahrefResponse = ahrefResponseObject[];

export type textResponse = {
  text: string;
};

export type textResponseObject = textResponse[];
